<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NuLigaViewer.Pages.SettingsPage"
             Title="Einstellungen">

    <Grid Padding="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Style="{StaticResource SettingsLeftLabelStyle}" Grid.Row="0" Grid.Column="0">Schriftbreite</Label>
        <Picker Grid.Row="0" Grid.Column="1" Title=""
                ItemsSource="{Binding FontWidths}"
                SelectedItem="{Binding FontWidth}" />
        <Label Style="{StaticResource SettingsLeftLabelStyle}" Grid.Row="1" Grid.Column="0">Jahr</Label>
        <Picker Grid.Row="1" Grid.Column="1" Title=""
                ItemsSource="{Binding Years}"
                SelectedItem="{Binding Year}" />
    </Grid>
</ContentPage>